fixtures:
  repositories:
    stdlib: 'git://github.com/puppetlabs/puppetlabs-stdlib.git'
    multi_validatre_re: 'git://github.com/nrvale0/puppet-postfix.git'
  symlinks:
    postfix: "#{source_dir}"
